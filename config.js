/* Optional: accounts & cross-device sync via Supabase (free tier).
   Leave both values empty and the site simply runs without accounts —
   progress still saves in each visitor's browser.
   Setup walkthrough: see README.md, "Accounts & sync" section. */
window.SUPABASE_CONFIG = {
  url: "",       // e.g. "https://abcdefgh.supabase.co"
  anonKey: "",   // the long "anon public" key from your project's API settings
};
